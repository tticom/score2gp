import xml.etree.ElementTree as ETimport zipfilefrom pathlib import Pathfrom score2gp.ir import ScoreIRfrom score2gp.gp_package import write_gp, validate_roundtripdef test_system_breaks_and_scaling_xml(tmp_path) -> None:    # 1. Load the system breaks and staff layout scaling synthetic test fixture    fixture_path = Path("fixtures/public/test_system_breaks.ir.json")    score = ScoreIR.from_json_file(fixture_path)    # 2. Write the ScoreIR to a GP7 binary package    out_gp = tmp_path / "system_breaks.gp"    warnings = write_gp(score, out_gp)    # Verify compile success with zero warnings    assert warnings == []    assert zipfile.is_zipfile(out_gp)    # 3. Unpack and extract Content/score.gpif to verify layout tags structurally    with zipfile.ZipFile(out_gp) as zf:        xml_content = zf.read("Content/score.gpif")        root = ET.fromstring(xml_content)        # Retrieve layouts under Score        layout_node = root.find(".//Score/Layout")        assert layout_node is not None        # SystemLayout checks        sys_lay = layout_node.find("SystemLayout")        assert sys_lay is not None        assert sys_lay.find("SystemSizePercent").text == "90.0"        assert sys_lay.find("StaffDistancingCushion").text == "12.5"        assert sys_lay.find("BarlineStyle").text == "Dashed"        # StaffLayout checks        staff_lay = layout_node.find("StaffLayout")        assert staff_lay is not None        assert staff_lay.find("StaffSpacingCushion").text == "8.0"        assert staff_lay.find("StaffSize").text == "1.1"        # Retrieve MasterBars        master_bars = root.findall(".//MasterBars/MasterBar")        mb_map = {int(mb.get("index")): mb for mb in master_bars}        # Bar 1: Barline is Hidden, layout break is Line        mb1 = mb_map[1]        assert mb1.find("Barline").text == "Hidden"        assert mb1.find("Break").text == "Line"        # Bar 2: Barline is Dashed, layout break is Page        mb2 = mb_map[2]        assert mb2.find("Barline").text == "Dashed"        assert mb2.find("Break").text == "Page"        # Retrieve Bars        bars = root.findall(".//Bars/Bar")        b_map = {int(b.get("index")): b for b in bars}        # Bar 1: LayoutBreak is System        b1 = b_map[1]        lb1 = b1.find("LayoutBreak")        assert lb1 is not None        assert lb1.find("Type").text == "System"        # Bar 2: LayoutBreak is Page        b2 = b_map[2]        lb2 = b2.find("LayoutBreak")        assert lb2 is not None        assert lb2.find("Type").text == "Page"    # 4. Validate round-trip extraction    res = validate_roundtrip(out_gp, score)    assert res["valid"], f"Round-trip validation failed: {res['errors']}"def test_layout_and_title_propagation(tmp_path):    from score2gp.deterministic_musicxml import generate_musicxml_sidecar    from score2gp.ir import ScoreIR    from score2gp.gp_package import write_gp    from unittest.mock import patch    # 1. Create a mock PDF file (must be > 10 bytes)    pdf_path = tmp_path / "test_trace.pdf"    pdf_path.write_text("fake pdf data that is longer than 10 bytes")    # 2. Mock OMR output    synthetic_res = {        "timeline_preview": [{            "page_index": 1,            "system_index": 1,            "staff_index": 1,            "measures": [                {                    "measure_index": 0,                    "events": [                        {"candidate_id": "note_1", "symbol_type": "quarter_note_candidate", "voice": 1, "start_tick": 0, "duration_ticks": 960, "resolved_pitch": "C4"}                    ]                }            ]        }, {            "page_index": 1,            "system_index": 2,            "staff_index": 1,            "measures": [                {                    "measure_index": 1,                    "section_name": "Exercise 1",                    "events": [                        {"candidate_id": "note_2", "symbol_type": "quarter_note_candidate", "voice": 1, "start_tick": 0, "duration_ticks": 960, "resolved_pitch": "E4"}                    ]                }            ]        }]    }    # 3. Generate sidecar MusicXML    out_xml = tmp_path / "test_trace.musicxml"    with patch("score2gp.deterministic_musicxml.run_recognition_on_file", return_value=synthetic_res):        generate_musicxml_sidecar(pdf_path, out_xml)    # 4. Verify MusicXML elements    assert out_xml.exists()    tree = ET.parse(out_xml)    root = tree.getroot()    measures_xml = root.findall(".//measure")    assert len(measures_xml) == 2    # Measure 1: Negative case    m1_xml = measures_xml[0]    print_tag_1 = m1_xml.find("print")    assert print_tag_1 is None    # Measure 2: Positive case    m2_xml = measures_xml[1]    print_tag = m2_xml.find("print")    assert print_tag is not None    assert print_tag.get("new-system") == "yes"    rehearsal_tag = m2_xml.find(".//direction-type/rehearsal")    assert rehearsal_tag is not None    assert rehearsal_tag.text == "Exercise 1"    # 5. Parse MusicXML to ScoreIR    from score2gp.musicxml import parse_musicxml    from score2gp.build_ir import build_ir_with_diagnostics_from_imports    from score2gp.tabraw import TabRaw    parsed_part = parse_musicxml(out_xml)    tabraw = TabRaw(candidates=[], warnings=[], pdf_layout_class="standard_staff")    score_ir, _ = build_ir_with_diagnostics_from_imports(parsed_part, tabraw)    assert len(score_ir.bars) == 2    b1 = score_ir.bars[0]    b2 = score_ir.bars[1]    assert b1.layout_break is None or b1.layout_break == "none"    assert b1.marker is None    assert b2.layout_break == "line"    assert b2.marker == "Exercise 1"    # 6. Write ScoreIR to GP7 package    out_gp = tmp_path / "test_trace.gp"    warnings = write_gp(score_ir, out_gp)    assert warnings == []    # Verify GP package content    import zipfile    with zipfile.ZipFile(out_gp) as zf:        xml_content = zf.read("Content/score.gpif")        gpif_root = ET.fromstring(xml_content)        master_bars = gpif_root.findall(".//MasterBars/MasterBar")        assert len(master_bars) == 2        mb1 = master_bars[0]        break_node_1 = mb1.find("Break")        assert break_node_1 is None or break_node_1.text == "None"        assert mb1.find("Section") is None        mb2 = master_bars[1]        assert mb2.find("Break").text == "Line"        sect_node = mb2.find("Section")        assert sect_node is not None        assert sect_node.find("Name").text == "Exercise 1"